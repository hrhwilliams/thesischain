# End2 WASM client

Web client for interacting with the End2 end-to-end encrypted messaging service.

## Building

Run the following command to build for webpack:

```
wasm-pack build --target bundler
```

And then serve using npm:

```
npm run serve
```